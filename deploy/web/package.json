{
  "name": "texer-web",
  "version": "1.0.0",
  "description": "TeXer - Formula screenshot to LaTeX converter (browser)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "katex": "^0.16.0",
    "onnxruntime-web": "^1.21.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
